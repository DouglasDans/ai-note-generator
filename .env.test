# Banco separado do de desenvolvimento — os testes de tests/db/ fazem
# beforeEach(prisma.space.deleteMany()), e isso já apagou dado de dev criado
# na mão uma vez. Ver vitest.setup.ts (carregado com override antes do
# client.ts carregar o .env normal).
DATABASE_URL="postgresql://ai_note_generator:ai_note_generator@localhost:5434/ai_note_generator_test"
